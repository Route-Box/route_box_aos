<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View" />
        <variable
            name="viewModel"
            type="com.daval.routebox.presentation.ui.route.RouteViewModel" />
        <variable
            name="editViewModel"
            type="com.daval.routebox.presentation.ui.route.edit.RouteEditViewModel" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <fragment
                class="com.google.android.gms.maps.SupportMapFragment"
                android:id="@+id/tracking_map"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/add_cv"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:backgroundTint="@color/sub_black"
                android:layout_marginBottom="60dp"
                android:layout_marginHorizontal="22dp"
                android:visibility="@{viewModel.route.routeActivities.size() == 0 ? View.VISIBLE : View.GONE}"
                app:cardCornerRadius="12dp"
                app:cardElevation="0dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/add_activity"
                    android:textColor="@color/white"
                    android:layout_marginHorizontal="22dp"
                    android:layout_marginVertical="20dp"
                    android:drawableEnd="@drawable/ic_plus"
                    style="@style/body_b_m" />

            </com.google.android.material.card.MaterialCardView>

        </androidx.constraintlayout.widget.ConstraintLayout>

        <include
            android:id="@+id/route_write_activity_bottom_sheet"
            layout="@layout/bottom_sheet_activity"
            android:visibility="@{viewModel.route.routeActivities.size() > 0 ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>